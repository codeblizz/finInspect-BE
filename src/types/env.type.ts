export type EnvType = {
  MONGODB_URL: string;
  PORT: number;
  HOST: string;
  NODE_ENV: string;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_HOSTNAME: string;
  DB_CLUSTER_NAME: string;
  DEBUG_LEVEL: number;
  REDIS_HOST: string;
  REDIS_PASSWORD: string;
  REDIS_PORT: number;
  REDIS_FAMILY: number;
  MAX_REDIS_RETRY: number;
  SECRET_KEY: string;
  JWT_SECRET: string;
  DB_NAME: string;
};
